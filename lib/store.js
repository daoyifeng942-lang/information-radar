const globalStore=globalThis.__INFORMATION_RADAR__||(globalThis.__INFORMATION_RADAR__=[]);
export function listItems(category){return globalStore.filter(x=>!category||x.category===category).sort((a,b)=>(b.score||0)-(a.score||0));}
export function saveItems(items){const keys=new Set(globalStore.map(x=>x.url||x.title));for(const x of items){const k=x.url||x.title;if(k&&!keys.has(k)){globalStore.push(x);keys.add(k)}}return listItems();}