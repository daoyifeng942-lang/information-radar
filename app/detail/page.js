'use client';
import {useEffect,useState} from 'react';
const tips={文化:'文化信息建议优先关注审美、诗性、原创性、精神深度、历史意义与能否形成长期文化记忆；实用性不是首要标准。',科技:'科技信息建议重点判断技术突破、产业影响、可信度、创新程度、可验证性与未来影响。',社会:'社会信息建议重点判断公共影响、事实可靠性、时效性、结构性意义与对理解社会变化的帮助。'};
export default function Detail(){const [item,setItem]=useState(null),[liked,setLiked]=useState(null);useEffect(()=>{const url=new URLSearchParams(location.search).get('url');fetch('/api/items',{cache:'no-store'}).then(r=>r.json()).then(j=>{const x=(j.items||[]).find(a=>a.url===url);setItem(x||{title:'信息已不在当前扫描结果中',url,category:'综合',source:'—',summary:'该条目可能来自上一轮扫描。你仍可以尝试访问原文。'})});},[]);if(!item)return <main className="detail-page"><div className="empty">正在打开信息详情…</div></main>;const category=item.category||'综合';return <main className="detail-page"><a className="back" href="/">← 返回信息雷达</a><div className="detail-meta">{category}　·　{item.source||'未知来源'}　·　{item.publishedAt||'近期'}</div><h1>{item.title}</h1><div className="detail-scores"><span>价值 <b>{Number(item.score??item.value??0).toFixed(1)}</b></span><span>时效 <b>{Number(item.timeliness??item.freshness??0).toFixed(1)}</b></span><span>紧急 <b>{Number(item.urgency??0).toFixed(1)}</b></span></div><section className="detail-section"><h2>内容概要</h2><p>{item.summary||'暂无摘要。建议进入原文查看完整内容。'}</p></section><section className="detail-section"><h2>为什么推荐</h2><p>{item.reason||'这条信息进入当前雷达，是因为它具备一定的来源优先级、时效性或主题价值。AI 推荐理由将在接入模型后进一步细化。'}</p></section><section className="detail-section"><h2>我的判断</h2><div className="feedback"><button className={liked===true?'chosen':''} onClick={()=>setLiked(true)}>♡ 喜欢</button><button className={liked===false?'chosen':''} onClick={()=>setLiked(false)}>♢ 不喜欢</button></div><p className="muted">你的选择可用于后续优化个人信息偏好。</p></section><section className="detail-section advice"><h2>这类信息怎么筛选？</h2><p>{tips[category]||'建议综合判断来源可信度、时效、信息密度、独特性与长期价值。'}</p></section><a className="original" href={item.url} target="_blank" rel="noreferrer">阅读原文 ↗</a></main>}
