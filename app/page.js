'use client';
import {useEffect,useMemo,useState} from 'react';import {todayQuote} from '../lib/quotes.js';
const tabs=['今日','文化','科技','社会'];const categoryTabs=['全部','文化','科技','社会'];
export default function Page(){const [tab,setTab]=useState('今日'),[filter,setFilter]=useState('全部'),[items,setItems]=useState([]),[loading,setLoading]=useState(true),[urgentOnly,setUrgentOnly]=useState(false),[sort,setSort]=useState('value'),[source,setSource]=useState('all'),[lastUpdated,setLastUpdated]=useState('');const [quote,setQuote]=useState(null);
async function load(){setLoading(true);try{const q=tab==='今日'?'':'?category='+encodeURIComponent(tab);const r=await fetch('/api/items'+q,{cache:'no-store'});const j=await r.json();setItems(j.items||[]);setLastUpdated(new Date().toLocaleString('zh-CN',{month:'numeric',day:'numeric',hour:'2-digit',minute:'2-digit'}))}catch(e){setItems([])}finally{setLoading(false)}}useEffect(()=>{load();setQuote(todayQuote())},[tab]);
const sources=useMemo(()=>['all',...new Set(items.map(x=>x.source).filter(Boolean))],[items]);const visible=useMemo(()=>{let list=items.filter(x=>filter==='全部'||x.category===filter);if(urgentOnly)list=list.filter(x=>Number(x.urgency||0)>=7);if(source!=='all')list=list.filter(x=>x.source===source);return [...list].sort((a,b)=>sort==='urgency'?Number(b.urgency||0)-Number(a.urgency||0):sort==='fresh'?Number(b.timeliness||b.freshness||0)-Number(a.timeliness||a.freshness||0):Number(b.score??b.value??0)-Number(a.score??a.value??0))},[items,filter,urgentOnly,source,sort]);
async function scan(){setLoading(true);try{const custom=JSON.parse(localStorage.getItem('radar-custom-sources')||'[]');await fetch('/api/scan',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({sources:custom}),cache:'no-store'});await load()}finally{setLoading(false)}}
return <main><header className="topbar"><div><div className="eyebrow">INFORMATION RADAR</div><h1>今日信息雷达</h1><p>指定来源优先 · AI 筛选 · 高价值信息 · 原文直达</p></div><div className="header-actions"><a className="source-manage" href="/sources">来源管理</a><button className="scan" onClick={scan} disabled={loading}>{loading?'扫描中…':'↻ 立即扫描'}</button><span className="updated">◷ {lastUpdated||'尚未扫描'}</span></div></header><nav className="main-tabs">{tabs.map(t=><button key={t} className={tab===t?'active':''} onClick={()=>{setTab(t);setFilter(t==='今日'?'全部':t)}}>{t}</button>)}</nav>{quote&&<section className="quote-of-day"><div className="quote-label">每日一句 · CLASSIC LINE</div><div className="quote-cn">“{quote.cn}”</div><div className="quote-en">{quote.quote}</div><div className="quote-note">{quote.note}</div><div className="quote-author">—— {quote.author} · {quote.work} <span>{quote.en}</span></div></section>}<section className="toolbar"><div className="filters">{categoryTabs.map(t=><button key={t} className={filter===t?'pill active-pill':'pill'} onClick={()=>setFilter(t)}>{t}</button>)}<span className="divider"/><label className="select">排序<select value={sort} onChange={e=>setSort(e.target.value)}><option value="value">价值最高</option><option value="fresh">时效最高</option><option value="urgency">紧急最高</option></select></label><label className="select">来源<select value={source} onChange={e=>setSource(e.target.value)}><option value="all">全部来源</option>{sources.slice(1).map(s=><option key={s} value={s}>{s}</option>)}</select></label><label className="urgent"><input type="checkbox" checked={urgentOnly} onChange={e=>setUrgentOnly(e.target.checked)}/> 仅看紧急</label></div></section><section className="feed-head"><span>{tab} · {visible.length} 条</span><span>价值 / 时效 / 紧急度</span></section><section className="feed">{loading?<div className="empty">正在读取雷达…</div>:visible.length?visible.map((x,i)=><article className="feed-row" key={x.url||i} onClick={()=>x.url&&(window.location.href='/detail?url='+encodeURIComponent(x.url))}><div className={'dot '+String(x.category||'').toLowerCase()}/><div className="category-tag">{x.category||'综合'}</div><div className="story"><h2>{x.title}</h2><div className="source-line">{x.source||'未知来源'} · {x.time||x.publishedAt||'近期'}</div><p>{x.summary||x.reason||'点击进入详情，查看 AI 推荐原因与筛选建议。'}</p></div><div className="metrics"><div><small>价值</small><strong>{Number(x.score??x.value??0).toFixed(1)}</strong></div><div><small>时效</small><strong>{Number(x.timeliness??x.freshness??0).toFixed(1)}</strong></div><div><small>紧急</small><strong>{Number(x.urgency??0).toFixed(1)}</strong></div></div>{Number(x.urgency||0)>=7&&<span className="urgent-badge">紧急</span>}<a className="read" href={'/detail?url='+encodeURIComponent(x.url)} onClick={e=>e.stopPropagation()}>详情 ↗</a></article>):<div className="empty">尚无扫描结果。点击右上角“立即扫描”开始。</div>}</section><footer>信息雷达 V1 · 文化以美与诗性为核心；科技与社会采用独立评价体系。 <span><a href="/sources">管理信息源</a>　·　每日一句</span></footer></main>}
